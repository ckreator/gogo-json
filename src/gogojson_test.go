package gogojson

/*
const basicJson = `{
  "name": "John",
  "age": 34,
  "male": true
}`

func TestParser(t *testing.T) {
	assert := assert.New(t)
	parsed := GogoJson(basicJson)

	assert.Equal(parsed["name"], "John")
	assert.Equal(parsed["age"], 34)
	assert.Equal(parsed["male"], true)
}
*/
